# xlsx 表配置说明

将 Excel（.xlsx）工作簿按工作表导出为 JSON / XML / Lua / YCL 文件。

## 一、表名标记

工作表名以 `|` 后接标记名，标记名即导出文件名（可中文）：

| 工作表名 | 导出文件 |
| --- | --- |
| `\|英雄` | `英雄.json` |

- 同一标记名在多个工作簿重复时导出失败，报错提示来源文件。
- `<<` 合并表标记已禁用。

## 二、数据表

前两行为表头，第 3 行起为数据：

| ID | 名字 | 坐骑 | 生命 |
| --- | --- | --- | --- |
| key | string | int | int |
| 1 | 奥丁 | 10001 | 1000 |

- 第 1 行字段名、第 2 行字段类型（`none` 的列整列不导出）。
- A 列强校验：A1 必须为 `ID`、A2 必须为 `key`，否则报错。
- A 列为 key，导出为**字典**（数字 `1.0` 归一为 `"1"`）：

```json
{
  "1": { "名字": "奥丁", "坐骑": 10001, "生命": 1000 }
}
```

A 列行级控制：

| A 列写法 | 效果 |
| --- | --- |
| `!server!xxx` | `-s server` 时跳过该行；其他签名导出，key 取 `xxx` |
| `#任意` | 注释行，跳过 |
| 空行 | 连续 3 个空行结束数据 |

key 重复时跳过整表，无数据则不生成文件。

## 三、配置表

首行含 `name`、`value`、`type` 三列即按配置表导出，结果为对象：

| name | value | type | sign | description |
| --- | --- | --- | --- | --- |
| NameLimit | 7 | int | server | 名字字数限制 |
| LevelRange | 1;99 | {int Min;int Max} | | 等级区间 |

- `sign` / `description` 列可选；`#` 开头 name 行跳过。
- 输出：`{ "NameLimit": 7, "LevelRange": { "Min": 1, "Max": 99 } }`

## 四、字段类型

| 类型 | 说明 | 示例值 |
| --- | --- | --- |
| `int` / `long` | 整数 | `100` |
| `float` | 浮点数 | `1.5` |
| `string` | 字符串 | `奥丁` |
| `bool` | `1/0`、`true/false`、`yes/no`、`on/off` | `1` |
| `T[]` | 列表，逗号分隔 | `1,2,3` |
| `{int Min;int Max}` | 对象，字段以 `;` 分隔（`-r` 可改） | `1;99` |
| `int(Mount.Id)` | 绑定类型，引用其他表（仅 `int`/`string`/`long`） | `10001` |
| `none` | 整列不导出 | |

对象内同样支持基础与列表类型，如 `{int[] Ids;string Name}`。

## 五、字符串转义

| 写法 | 含义 |
| --- | --- |
| `\n` | 换行 |
| `\,` | 逗号 |
| `\` + 对象分隔符（默认 `;`） | 对象分隔符 |

## 六、命令行参数

```bash
python 导表入口.py -p 文件列表 -f 输出目录 -e 格式 [选项]
```

| 参数 | 说明 |
| --- | --- |
| `-p` | 输入 xlsx，`,` `;` `\|` 分隔 |
| `-f` | 输出目录 |
| `-e` | json / xml / lua / ycl |
| `-s` | 签名，按行筛选 |
| `-t` | 导出文件名后缀（如 `-t Config` → `英雄Config.json`） |
| `-r` | 对象字段分隔符，默认 `;` |
| `-m` | 多进程数量，默认 CPU 核数 |
| `-c` | 输出表结构 schema 文件（代码生成用） |

`-s` 签名匹配：单元格签名按 `/` `\` `,` 空格 `:` 分隔，任一子项含于 `-s` 参数即导出（`-s` 缺省时全部导出）。

## 七、快速模板

数据表（工作表名 `|英雄`）：

| ID | 名字 | 武器 | 属性 |
| --- | --- | --- | --- |
| key | string | int[] | {int Min;int Max} |
| 1 | 奥丁 | 1001,1002 | 1;99 |

配置表：

| name | value | type | sign | description |
| --- | --- | --- | --- | --- |
| NameLimit | 7 | int | server | 名字字数限制 |