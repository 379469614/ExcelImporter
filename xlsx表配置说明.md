# xlsx 表配置说明

本工具用于将 Excel（.xlsx）配置表导出为 JSON / XML / Lua / YCL 文件。一个工作簿内可包含多个工作表，按工作表名称中的 `|` 标记决定是否导出。

---

## 一、表名标记规则

工作表名称中含 **`|` + 标记名** 即会导出，`|` 前的部分为中文注释、不参与导出。

| 工作表名称 | 是否导出 | 导出文件名 |
| --- | --- | --- |
| `|英雄` | ✅ 导出 | `英雄.json` |
| `英雄\|Hero` | ✅ 导出 | `Hero.json`（兼容旧写法，取 `\|` 后标记） |
| `英雄` | ❌ 不导出 | — |
| `|英雄<<` | ✅ 导出（合并表） | 合并到主文件 |

- **导出文件名 = 标记名**，不再追加复数 `s`。
- 标记名支持中文（如 `|英雄` → `英雄.json`）。

---

## 二、数据表格式（两行头）

> 第一行 = 字段名（支持中文） → 第二行 = 字段类型 → 第三行起 = 正式数据

| A | B | C |
| --- | --- | --- |
| **名字** | **坐骑** | **生命** | ← 第 1 行：字段名（可中文） |
| **string** | **int** | **int** | ← 第 2 行：字段类型 |
| 奥丁 | 10001 | 1000 | ← 第 3 行起：数据 |
| 托尔 | 10002 | 2000 | |

导出结果（`英雄.json`）：

```json
[
  { "名字": "奥丁", "坐骑": 10001, "生命": 1000 },
  { "名字": "托尔", "坐骑": 10002, "生命": 2000 }
]
```

注意：

- 已移除旧版的"说明行"与"签名行"（原第 1、4 行），**不需要也不能再填写**。
- 字段名不允许出现空值与注释符 `#`。
- 连续 3 个空行视为数据结束。
- `#` 开头的行视为注释，跳过。
- 表内无有效字段时会导出空数组。

### 行级控制（可选）

行首的 `!` 可控制该行是否导出：

| 写法 | 效果 |
| --- | --- |
| `!server!xxx` | `-s server` 导出时跳过此整行；其他签名导出时，首列取 `!server!` 之后的文本 |
| `#任意内容` | 注释行，跳过 |
| 空行 | 连续 3 个空行结束数据 |

---

## 三、字段类型

### 基础类型

| 类型 | 说明 | 示例 |
| --- | --- | --- |
| `int` | 整数 | `100` |
| `long` | 长整数 | `100000` |
| `double` | 浮点数 | `1.5` |
| `float` | 浮点数 | `1.5` |
| `string` | 字符串 | `奥丁` |
| `bool` | 布尔（`1/0`、`true/false`、`yes/no`、`on/off`） | `1` |

### 列表类型

类型写作 **`基础类型[]`**，值为英文逗号分隔，自动根据基础类型转换：

| 类型 | 值示例 | 导出结果 |
| --- | --- | --- |
| `int[]` | `1,2,3` | `[1, 2, 3]` |
| `string[]` | `奥丁,托尔` | `["奥丁", "托尔"]` |

> 字段名自动复数化：`Weapon`（int[]）导出为 `Weapons`（`-x` 参数可关闭）。

### 对象类型

类型写作 **`{字段类型 字段名;...}`**，对象字段以 `;` 分隔（`-r` 参数可改）：

| 类型 | 值示例 | 导出结果 |
| --- | --- | --- |
| `{int Min;int Max}` | `1;99` | `{"Min": 1, "Max": 99}` |

对象内同样支持基础类型与列表类型，如 `{int[] Ids;string Name}`。

### 绑定类型（外键引用）

类型写作 **`int(标记.字段)`**，用于声明该字段引用另一张表：

| 类型 | 值示例 | 说明 |
| --- | --- | --- |
| `int(Mount.Id)` | `10001` | 引用 `Mount` 表的 `Id` 字段 |

绑定类型值在导出数据中仍为普通基础类型，仅用于 `-c` 代码生成模式生成引用结构。

---

## 四、字符串转义

`string` 类型单元格内支持转义：

| 写法 | 含义 |
| --- | --- |
| `\n` | 换行符 |
| `\,` | 逗号（避免被列表分割误判） |
| `\;` | 分号（对象分隔符，`-r` 可改） |

---

## 五、配置表格式（可选）

若工作表首行为 `name / value / type / sign / description` 标题，则按**配置表**导出，结果为对象而非数组：

| name | value | type | sign | description |
| --- | --- | --- | --- | --- |
| NameLimit | 7 | int | client/server | 名字字数限制 |
| LevelRange | 1;99 | {int Min;int Max} | client/server | 等级区间 |

导出结果示例（`Global.json`）：

```json
{
  "NameLimit": 7,
  "LevelRange": { "Min": 1, "Max": 99 }
}
```

- `sign` 列控制行级签名（`-s` 参数筛选，可留空 = 全部导出）。
- `#` 开头的 name 行视为注释，跳过。
- `description` 列用于 `-c` 代码生成模式。

> 提示：若某表首行恰好含有 `name`、`value`、`type` 三列表头，会被识别为配置表；普通数据表请避免这三种列名组合。

---

## 六、命令行参数

```bash
python 导表入口.py -p 文件列表 -f 输出目录 -e 格式 [选项]
```

| 参数 | 说明 |
| --- | --- |
| `-p` | 输入 xlsx 文件，用 `,` `;` `|` 分隔 |
| `-f` | 输出目录 |
| `-e` | 导出格式：`json` / `xml` / `lua` / `ycl` |
| `-s` | 签名，控制导出哪些行/列（默认全部导出） |
| `-t` | 导出文件名后缀（如 `Config` → `英雄Config.json`） |
| `-r` | 对象字段分隔符，默认 `;` |
| `-m` | 多进程数量，默认 CPU 核数 |
| `-c` | 输出表结构 schema 文件路径（供代码生成器使用） |
| `-x` | 关闭自动复数命名（列表字段名不追加 `s`） |

示例：

```bash
python 导表入口.py -p "表1.xlsx,表2.xlsx" -f ./out -e json
```

---

## 七、快速模板

### 数据表模板（复制到 Excel）

| 名字 | 坐骑 | 生命 |
| --- | --- | --- |
| string | int | int |
| 奥丁 | 10001 | 1000 |
| 托尔 | 10002 | 2000 |

工作表名称改为 `|英雄`，导出即得 `英雄.json`。

### 含列表与对象的模板

| Id | 名字 | 武器 | 属性 |
| --- | --- | --- | --- |
| int | string | int[] | {int Min;int Max} |
| 1 | 奥丁 | 1001,1002 | 1;99 |